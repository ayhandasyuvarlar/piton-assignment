import { notFound } from "next/navigation";

const Category = ({ params, searchParams }) => {
  // const category = "metafizik";
  // if (searchParams.product !== category) {
  //   notFound();
  // }
  return <div>hell word</div>;
};

export default Category;
